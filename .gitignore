# ─────────────────────────────────────────────────────────────────────────
# Game assets — NEVER commit. This repo ships tooling, not Epic Seven content.
# Bring your own extracted assets; the pipeline populates these directories.
# ─────────────────────────────────────────────────────────────────────────
site/assets/
# Ignore generated game data, EXCEPT the two hand-authored tooling config files
# (slot-name strip/exclude patterns — not game content; the bake + viewer need
# them). `site/data/*` (not `site/data/`) so the `!` re-includes can take effect.
site/data/*
!site/data/strip_patterns.json
!site/data/slot_excludes.json
site/og-card.png
*.scsp
*.sct
*.atlas
*.spine

# Cached third-party runtime — fetch from Esoteric on setup (see README)
spine-player.js
spine-player.css
**/spine-player.js
**/spine-player.css

# Community datasets — fetch your own (see README / CREDITS.md)
data_external/
HeroDatabase.json
HeroSkins.json
HeroNames_rtastats.json

# Deploy / infra artifacts & logs
*.log
r2_files.txt
.wrangler/
cors_rules.json

# Reverse-engineering artifacts (game-derived — do not publish)
*.bin
tools/Dump*.java
_game_re/

# Decryption key material — NEVER commit. Bring your own (see README).
# *.bin above already covers the outer-XOR key file.
*.pass
tools/voice_keys.json

# Voice pipeline — game-derived audio + the FSB5 decoder (fetched, not vendored)
site/voice/
site/data/voices.json
site/data/voices_audio.json
_voice_work/
tools/vendor/vgmstream/

# Python / Node
__pycache__/
*.pyc
node_modules/

# Local scratch
_*.json
_archive/
.spike/
_combat_backup_*/
ml_candidate_map.txt
